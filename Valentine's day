<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page title</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-image: url('address of the picture.jpg');
            background-size: cover;
            background-position: center;
            color: #333;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            position: relative;
        }
        #question-box {
            background-color: rgba(255, 255, 255, 0.8); 
            padding: 40px; 
            border-radius: 20px; 
            text-align: center;
            margin-bottom: 20px;
            position: relative; 
            height: 80px; 
        }
        h1 {
            margin-bottom: 20px;
            margin-top: 0; 
        }
        button {
            padding: 10px 20px;
            margin: 5px;
            font-size: 16px;
            border: none;
            cursor: pointer;
            background-color: #ff4d4d;
            color: white;
            border-radius: 5px;
            transition: transform 0.3s ease;
            transform-origin: center; 
            width: 100px; 
            height: 40px; 
            position: absolute; 
        }
        button:hover {
            transform: scale(1.05);
        }
        #yes-btn {
            bottom: 10px;
            right: calc(50% + 10px);
        }
        #no-btn {
            bottom: 10px;
            left: calc(50% + 10px);
        }
        .button-group {
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.5); 
        }
        .modal-content {
            background-color: #fefefe;
            margin: 15% auto;
            padding: 20px;
            border: 1px solid #888;
            width: 80%;
            border-radius: 10px;
            text-align: center;
        }
        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }
        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div id="question-box">
        <h1>question</h1>
        <div class="button-group">
            <button id="yes-btn">Yes</button>
            <button id="no-btn">No</button>
        </div>
    </div>
    <div id="myModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <h2>Simple text</h2>
            <img src="Your photo" alt="Your photo">
        </div>
    </div>

    <script>
        document.getElementById('no-btn').addEventListener('mouseover', function() {
            const questionBox = document.getElementById('question-box');
            const width = questionBox.clientWidth - this.clientWidth;
            const height = questionBox.clientHeight - this.clientHeight;
            const newPosX = Math.random() * width;
            const newPosY = Math.random() * height;
            this.style.left = newPosX + 'px';
            this.style.top = newPosY + 'px';
        });

        var modal = document.getElementById("myModal");

        var btn = document.getElementById("yes-btn");

        var span = document.getElementsByClassName("close")[0];

        btn.onclick = function() {
            modal.style.display = "block";
        }

        span.onclick = function() {
            modal.style.display = "none";
        }

        window.onclick = function(event) {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }
    </script>
</body>
</html>
